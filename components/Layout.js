const Layout = ({ children }) => {
    return (
        <main className="
            font-JetBrains 
            max-w-[500px]
            w-[90%]
            m-auto
            my-10
        ">
            {children}
        </main>
    )
}

export default Layout;